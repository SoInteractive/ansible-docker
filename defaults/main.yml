---
#docker_package: "docker-engine=1.10.3-0~trusty"

docker_directory_path: ""

docker_registries:
  - "registry.isaacloud.com"

docker_socket: "unix:///var/run/docker.sock"

docker_disable_iptables: false

docker_containers: []

docker_unprivileged_access: false
docker_telegraf_monitoring: false
