{
{% if docker_directory_path != "" %}
    "graph": "{{ docker_directory_path }}",
{% endif %}
    "insecure-registries": [
{% for registry in docker_registries %}
         {% if loop.index != 1 %},{% endif %}"{{ registry }}"
{% endfor %}
    ]
}
